import React from 'react';
import Styled{{pascalCase name}} from './Styled{{pascalCase name}}.ts';


interface {{pascalCase name}}Props {
  // Define here props of this Component
}

function {{pascalCase name}}(props: {{pascalCase name}}Props): React.ReactNode {
  const {
    // someProps
  } = props;

  return (
    <Styled{{pascalCase name}} >
      {{pascalCase name}} Component!
    </Styled{{pascalCase name}}>
  );
}

export default {{pascalCase name}};
